{
  "name": "ibal-tanques-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:all": "concurrently \"cd backend && php artisan serve --host=127.0.0.1 --port=8001\" \"node ws-server.js\" \"cd frontend && npm run dev -- --host 127.0.0.1\" -k --names backend,ws,frontend --prefix name"
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  },
  "dependencies": {
    "ws": "^8.21.3"
  }
}
